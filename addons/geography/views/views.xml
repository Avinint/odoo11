<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="geography.list">
      <field name="name">geography list</field>
      <field name="model">geography.geography</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="geography.action_window">
      <field name="name">geography window</field>
      <field name="res_model">geography.geography</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="geography.action_server">
      <field name="name">geography server</field>
      <field name="model_id" ref="model_geography_geography"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="geography" id="geography.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="geography.menu_1" parent="geography.menu_root"/>
    <menuitem name="Menu 2" id="geography.menu_2" parent="geography.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="geography.menu_1_list" parent="geography.menu_1"
              action="geography.action_window"/>
    <menuitem name="Server to list" id="geography" parent="geography.menu_2"
              action="geography.action_server"/>
    -->
  </data>
</odoo>